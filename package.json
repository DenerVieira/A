{
  "name": "sptrans-bus-tracker",
  "version": "1.0.0",
  "description": "Bus tracking using SPTrans API and Leaflet via Netlify Function proxy.",
  "main": "netlify/functions/sptrans-proxy.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "netlify",
    "sptrans",
    "leaflet"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "node-fetch": "^2.6.1" 
    /* Nota: Versão 2.6.1 é amplamente compatível com Netlify Functions */
  }
}
